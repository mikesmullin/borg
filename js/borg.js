// Generated by CoffeeScript 1.4.0
var cmdr, pack, ssh;

ssh = require('./ssh');

cmdr = require('commander');

pack = require('../package.json');

cmdr.version(pack.version).command('rekey [nodes]').description('copy ssh public key to authorized_hosts on remote machine(s)').action(function(nodes) {
  return console.log('setup for %j', nodes);
});

cmdr.parse(process.argv);
